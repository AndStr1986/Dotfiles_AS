# Dotfiles

## Setup

## Step 1.
git clone git@github.com:anders/Dotfiles_AS.git ~/Dotfiles_AS

## Step 2.
cd ~/Dotfiles_AS

## Step 3.
./bootstrap.sh

## Install packages
brew bundle --file=~/Dotfiles_AS/Brewfile

## FZF keybind
$(brew --prefix)/opt/fzf/install

# add to zsh/.zshrc

